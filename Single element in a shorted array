class Solution {
public:
    int singleNonDuplicate(vector<int>& nums) {
        int i=0;
        while(i+1<nums.size()&&nums[i]==nums[i+1]){
            i+=2;
        }
        return nums[i];
    }
};
